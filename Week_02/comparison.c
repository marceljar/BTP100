#include <stdio.h>

int main() {
    int i = 7, j = 2;

    printf(" %i > %i => %i\n", i, j, i > j);
    printf(" %i <= %i => %i\n", i, j, i <= j);
    printf(" %i == %i => %i\n", i, j, i == j);
    
    return 0;
}
