#include <stdio.h>

int main() {

    int array[3];

    array[0] = 10; array[1] = 20; array[2] = 30;

    printf("array elements are: %i, %i, and %i\n", 
                      array[0], array[1], array[2]);

    return 0;
}